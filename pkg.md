# Packages

todo: This will be a list of packages to install so in future we can create a script to spin up new env

## To dump actually installed packages:

```sh
brew leaves --installed-on-request | xargs -n1 brew desc
```

## To install:

brew install bat
brew install fd
brew install direnv
brew install fzf
brew install gh
brew install git-delta
brew install neovim
brew install ripgrep
brew install tmux
brew install tree
brew install prettier
